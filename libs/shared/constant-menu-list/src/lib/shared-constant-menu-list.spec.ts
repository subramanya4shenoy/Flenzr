import { sharedConstantMenuList } from './shared-constant-menu-list';

describe('sharedConstantMenuList', () => {
    it('should work', () => {
        expect(sharedConstantMenuList()).toEqual('shared-constant-menu-list');
    })
})